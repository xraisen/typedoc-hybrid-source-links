# Workflow Benchmark

This benchmark describes the tested workflow around an AI assistant, not the intelligence of any model.

Tested gates:

- `typedoc:health`
- `typedoc:doctor`
- GraphRAG smart preflight route
- TypeDoc smart preflight route
- `ai:graph:build`
- `ai:graph:doctor`
- `ai:graph:check-leaks`
- `ai:spec` smoke test

Result: 8/8 workflow gates passed in the referenced local validation run.
